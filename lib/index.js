const { prepareChunkedCodeGenerator } = require('./prepare-code-maker')
const { randomNumbersSequence } = require('./random-sequence')

module.exports = { prepareChunkedCodeGenerator, randomNumbersSequence }