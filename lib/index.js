const { createGenerator } = require('./createGenerator')
const { sequenceFromAlphabet, numericSequence } = require('./sequence')

module.exports = {
  createGenerator,
  numericSequence,
  sequenceFromAlphabet
}
